# Documentation asset provenance

`mac-pulse-synthetic.svg` is a deterministic, hand-composed product
illustration created for this public repository. Every device value and
timestamp is synthetic. It does not contain a screenshot, export, or telemetry
from the private production dashboard.

The SVG source is the final export so its text, chart values, dimensions, and
privacy label remain reviewable in Git.
